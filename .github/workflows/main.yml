name: Deploy frontend app for PR review

on:
  pull_request:
    branches:
      - develop
    paths: 
      - "frontend/**"

jobs:
  check-style:
    runs-on: ubuntu-22.04

    steps:
     

      - name: Install dependencies and run linter
        working-directory: ./frontend
        run: |
          npm install
          npm run lint
